{
  "name": "@canonweave/action",
  "private": true,
  "version": "0.3.0",
  "description": "Canonweave gate as a GitHub Action: engine-backed gate, inline annotations on suspect artifact lines, one sticky PR comment updated in place, job summary, fork-safe degradation, CLI exit-code contract. Zero runtime dependencies.",
  "type": "module",
  "main": "index.mjs",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/canonweave/canonweave.git",
    "directory": "packages/action"
  },
  "license": "Apache-2.0"
}
