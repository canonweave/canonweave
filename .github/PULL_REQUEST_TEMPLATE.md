## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm test` passes (CLI selftest + action selftest + determinism + zero-deps — no install step)
- [ ] I kept the change scoped and small
- [ ] I updated docs if behaviour or setup changed
