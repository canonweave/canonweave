# canonweave.yml — repo config (see docs/file-format.md in the canonweave repo)
roots: [docs/trace]
ontology: docs/trace/ontology.yml
graph: docs/trace/graph.json
gate: ready-to-build
drafter:
  backend: template   # deterministic stub; switch to anthropic/openai when WS3 lands, or cmd for a local CLI
sync:
  issues: false
resolvers: []
